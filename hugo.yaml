baseurl: "https://confusedalex.dev"
languageCode: "en-us"
title: "confusedalex"
enableGitInfo: true
footnotereturnlinkcontents: "↩"

permalinks:
  posts: "/posts/:slug/"

params:
  giturl: "https://github.com/confusedalex/blog"

menu:
  main:
    - name: Home
      url: ""
      weight: 1
    - name: Posts
      url: "posts/"
      weight: 2
    - name: About
      url: "about/"
      weight: 3
    - name: Subscribe
      url: "index.xml"

markup:
  highlight:
    noClasses: false
  goldmark:
    renderer:
      # allows for injecting html
      unsafe: true
    extensions:
      passthrough:
        # allows for including latex
        enable: true
        delimiters:
          block:
            - - \[
              - \]
            - - $$
              - $$
          inline:
            - - \(
              - \)
